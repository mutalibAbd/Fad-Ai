-++++++++++++++++++++++++++++++++++++++++++# Code Citations

## License: unknown
https://github.com/calebrunion/next/blob/38008159318276be3b05f7309c479eeaf320d722/tailwind.config.ts

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FA
```


## License: unknown
https://github.com/incubateur-ademe/territoires-en-transitions/blob/5e0fd766548a65d036a2426f185010d96354d416/packages/site/tailwind.config.js

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FA
```


## License: unknown
https://github.com/calebrunion/next/blob/38008159318276be3b05f7309c479eeaf320d722/tailwind.config.ts

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FA
```


## License: unknown
https://github.com/incubateur-ademe/territoires-en-transitions/blob/5e0fd766548a65d036a2426f185010d96354d416/packages/site/tailwind.config.js

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FA
```


## License: unknown
https://github.com/calebrunion/next/blob/38008159318276be3b05f7309c479eeaf320d722/tailwind.config.ts

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FA
```


## License: unknown
https://github.com/incubateur-ademe/territoires-en-transitions/blob/5e0fd766548a65d036a2426f185010d96354d416/packages/site/tailwind.config.js

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FA
```


## License: unknown
https://github.com/calebrunion/next/blob/38008159318276be3b05f7309c479eeaf320d722/tailwind.config.ts

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FA
```


## License: unknown
https://github.com/incubateur-ademe/territoires-en-transitions/blob/5e0fd766548a65d036a2426f185010d96354d416/packages/site/tailwind.config.js

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FA
```


## License: unknown
https://github.com/calebrunion/next/blob/38008159318276be3b05f7309c479eeaf320d722/tailwind.config.ts

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FA
```


## License: unknown
https://github.com/incubateur-ademe/territoires-en-transitions/blob/5e0fd766548a65d036a2426f185010d96354d416/packages/site/tailwind.config.js

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FA
```


## License: unknown
https://github.com/calebrunion/next/blob/38008159318276be3b05f7309c479eeaf320d722/tailwind.config.ts

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FA
```


## License: unknown
https://github.com/incubateur-ademe/territoires-en-transitions/blob/5e0fd766548a65d036a2426f185010d96354d416/packages/site/tailwind.config.js

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FA
```


## License: unknown
https://github.com/incubateur-ademe/territoires-en-transitions/blob/5e0fd766548a65d036a2426f185010d96354d416/packages/site/tailwind.config.js

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FA
```


## License: unknown
https://github.com/calebrunion/next/blob/38008159318276be3b05f7309c479eeaf320d722/tailwind.config.ts

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FA
```


## License: Apache-2.0
https://github.com/LexSwed/fxtrot-ui/blob/932303a8c8225b9587dd222c3d0f50f8a72d1529/src/lib/stitches.config.ts

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FADAI Blue
        primary: {
          DEFAULT: '#2B59FF',
          50: '#F0F4FF',
          100: '#E0E9FF',
          200: '#C1D3FF',
          300: '#A2BDFF',
          400: '#6B8EFF',
          500: '#2B59FF', // Main primary
          600: '#1F3FD6',
          700: '#1428A0',
          800: '#0E176D',
          900: '#080D3A',
        },
        // Background Off-white
        background: {
          DEFAULT: '#FBFBFB',
          light: '#F5F5F7',
        },
        // Text colors
        text: {
          primary: '#1D1D1F',
          secondary: '#86868B',
        },
      },
      fontFamily: {
        sans: ['Inter', ...defaultTheme.fontFamily.sans],
      },
      fontSize: {
        // Use tracking-tight pattern
        xs: ['12px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        sm: ['14px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        base: ['16px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        lg: ['18px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        xl: ['20px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        '2xl': ['24px', { lineHeight: '1.4', letterSpacing: '-0.02em' }],
        '3xl': ['30px', { lineHeight: '1.3', letterSpacing: '-0.02em' }],
        '4xl': ['36px', { lineHeight: '1.2', letterSpacing: '-0.02em' }],
      },
      boxShadow: {
        // Card shadows (Xiaomi style)
        'sm': '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
        'md': '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
        'lg': '0 10px 15px -3px rgba
```


## License: Apache-2.0
https://github.com/LexSwed/fxtrot-ui/blob/932303a8c8225b9587dd222c3d0f50f8a72d1529/src/lib/stitches.config.ts

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FADAI Blue
        primary: {
          DEFAULT: '#2B59FF',
          50: '#F0F4FF',
          100: '#E0E9FF',
          200: '#C1D3FF',
          300: '#A2BDFF',
          400: '#6B8EFF',
          500: '#2B59FF', // Main primary
          600: '#1F3FD6',
          700: '#1428A0',
          800: '#0E176D',
          900: '#080D3A',
        },
        // Background Off-white
        background: {
          DEFAULT: '#FBFBFB',
          light: '#F5F5F7',
        },
        // Text colors
        text: {
          primary: '#1D1D1F',
          secondary: '#86868B',
        },
      },
      fontFamily: {
        sans: ['Inter', ...defaultTheme.fontFamily.sans],
      },
      fontSize: {
        // Use tracking-tight pattern
        xs: ['12px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        sm: ['14px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        base: ['16px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        lg: ['18px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        xl: ['20px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        '2xl': ['24px', { lineHeight: '1.4', letterSpacing: '-0.02em' }],
        '3xl': ['30px', { lineHeight: '1.3', letterSpacing: '-0.02em' }],
        '4xl': ['36px', { lineHeight: '1.2', letterSpacing: '-0.02em' }],
      },
      boxShadow: {
        // Card shadows (Xiaomi style)
        'sm': '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
        'md': '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
        'lg': '0 10px 15px -3px rgba
```


## License: unknown
https://github.com/zrrd/spring-boot-learn/blob/ca0ed64ce93dd11f2d62bec15050c78833e9739b/spring-boot-27-amis/src/main/resources/static/amis/scss/_variables.scss

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FADAI Blue
        primary: {
          DEFAULT: '#2B59FF',
          50: '#F0F4FF',
          100: '#E0E9FF',
          200: '#C1D3FF',
          300: '#A2BDFF',
          400: '#6B8EFF',
          500: '#2B59FF', // Main primary
          600: '#1F3FD6',
          700: '#1428A0',
          800: '#0E176D',
          900: '#080D3A',
        },
        // Background Off-white
        background: {
          DEFAULT: '#FBFBFB',
          light: '#F5F5F7',
        },
        // Text colors
        text: {
          primary: '#1D1D1F',
          secondary: '#86868B',
        },
      },
      fontFamily: {
        sans: ['Inter', ...defaultTheme.fontFamily.sans],
      },
      fontSize: {
        // Use tracking-tight pattern
        xs: ['12px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        sm: ['14px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        base: ['16px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        lg: ['18px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        xl: ['20px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        '2xl': ['24px', { lineHeight: '1.4', letterSpacing: '-0.02em' }],
        '3xl': ['30px', { lineHeight: '1.3', letterSpacing: '-0.02em' }],
        '4xl': ['36px', { lineHeight: '1.2', letterSpacing: '-0.02em' }],
      },
      boxShadow: {
        // Card shadows (Xiaomi style)
        'sm': '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
        'md': '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
        'lg': '0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2
```


## License: Apache-2.0
https://github.com/LexSwed/fxtrot-ui/blob/932303a8c8225b9587dd222c3d0f50f8a72d1529/src/lib/stitches.config.ts

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FADAI Blue
        primary: {
          DEFAULT: '#2B59FF',
          50: '#F0F4FF',
          100: '#E0E9FF',
          200: '#C1D3FF',
          300: '#A2BDFF',
          400: '#6B8EFF',
          500: '#2B59FF', // Main primary
          600: '#1F3FD6',
          700: '#1428A0',
          800: '#0E176D',
          900: '#080D3A',
        },
        // Background Off-white
        background: {
          DEFAULT: '#FBFBFB',
          light: '#F5F5F7',
        },
        // Text colors
        text: {
          primary: '#1D1D1F',
          secondary: '#86868B',
        },
      },
      fontFamily: {
        sans: ['Inter', ...defaultTheme.fontFamily.sans],
      },
      fontSize: {
        // Use tracking-tight pattern
        xs: ['12px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        sm: ['14px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        base: ['16px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        lg: ['18px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        xl: ['20px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        '2xl': ['24px', { lineHeight: '1.4', letterSpacing: '-0.02em' }],
        '3xl': ['30px', { lineHeight: '1.3', letterSpacing: '-0.02em' }],
        '4xl': ['36px', { lineHeight: '1.2', letterSpacing: '-0.02em' }],
      },
      boxShadow: {
        // Card shadows (Xiaomi style)
        'sm': '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
        'md': '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
        'lg': '0 10px 15px -3px rgba
```


## License: unknown
https://github.com/zrrd/spring-boot-learn/blob/ca0ed64ce93dd11f2d62bec15050c78833e9739b/spring-boot-27-amis/src/main/resources/static/amis/scss/_variables.scss

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FADAI Blue
        primary: {
          DEFAULT: '#2B59FF',
          50: '#F0F4FF',
          100: '#E0E9FF',
          200: '#C1D3FF',
          300: '#A2BDFF',
          400: '#6B8EFF',
          500: '#2B59FF', // Main primary
          600: '#1F3FD6',
          700: '#1428A0',
          800: '#0E176D',
          900: '#080D3A',
        },
        // Background Off-white
        background: {
          DEFAULT: '#FBFBFB',
          light: '#F5F5F7',
        },
        // Text colors
        text: {
          primary: '#1D1D1F',
          secondary: '#86868B',
        },
      },
      fontFamily: {
        sans: ['Inter', ...defaultTheme.fontFamily.sans],
      },
      fontSize: {
        // Use tracking-tight pattern
        xs: ['12px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        sm: ['14px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        base: ['16px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        lg: ['18px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        xl: ['20px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        '2xl': ['24px', { lineHeight: '1.4', letterSpacing: '-0.02em' }],
        '3xl': ['30px', { lineHeight: '1.3', letterSpacing: '-0.02em' }],
        '4xl': ['36px', { lineHeight: '1.2', letterSpacing: '-0.02em' }],
      },
      boxShadow: {
        // Card shadows (Xiaomi style)
        'sm': '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
        'md': '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
        'lg': '0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2
```


## License: Apache-2.0
https://github.com/LexSwed/fxtrot-ui/blob/932303a8c8225b9587dd222c3d0f50f8a72d1529/src/lib/stitches.config.ts

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FADAI Blue
        primary: {
          DEFAULT: '#2B59FF',
          50: '#F0F4FF',
          100: '#E0E9FF',
          200: '#C1D3FF',
          300: '#A2BDFF',
          400: '#6B8EFF',
          500: '#2B59FF', // Main primary
          600: '#1F3FD6',
          700: '#1428A0',
          800: '#0E176D',
          900: '#080D3A',
        },
        // Background Off-white
        background: {
          DEFAULT: '#FBFBFB',
          light: '#F5F5F7',
        },
        // Text colors
        text: {
          primary: '#1D1D1F',
          secondary: '#86868B',
        },
      },
      fontFamily: {
        sans: ['Inter', ...defaultTheme.fontFamily.sans],
      },
      fontSize: {
        // Use tracking-tight pattern
        xs: ['12px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        sm: ['14px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        base: ['16px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        lg: ['18px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        xl: ['20px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        '2xl': ['24px', { lineHeight: '1.4', letterSpacing: '-0.02em' }],
        '3xl': ['30px', { lineHeight: '1.3', letterSpacing: '-0.02em' }],
        '4xl': ['36px', { lineHeight: '1.2', letterSpacing: '-0.02em' }],
      },
      boxShadow: {
        // Card shadows (Xiaomi style)
        'sm': '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
        'md': '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
        'lg': '0 10px 15px -3px rgba
```


## License: unknown
https://github.com/zrrd/spring-boot-learn/blob/ca0ed64ce93dd11f2d62bec15050c78833e9739b/spring-boot-27-amis/src/main/resources/static/amis/scss/_variables.scss

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FADAI Blue
        primary: {
          DEFAULT: '#2B59FF',
          50: '#F0F4FF',
          100: '#E0E9FF',
          200: '#C1D3FF',
          300: '#A2BDFF',
          400: '#6B8EFF',
          500: '#2B59FF', // Main primary
          600: '#1F3FD6',
          700: '#1428A0',
          800: '#0E176D',
          900: '#080D3A',
        },
        // Background Off-white
        background: {
          DEFAULT: '#FBFBFB',
          light: '#F5F5F7',
        },
        // Text colors
        text: {
          primary: '#1D1D1F',
          secondary: '#86868B',
        },
      },
      fontFamily: {
        sans: ['Inter', ...defaultTheme.fontFamily.sans],
      },
      fontSize: {
        // Use tracking-tight pattern
        xs: ['12px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        sm: ['14px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        base: ['16px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        lg: ['18px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        xl: ['20px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        '2xl': ['24px', { lineHeight: '1.4', letterSpacing: '-0.02em' }],
        '3xl': ['30px', { lineHeight: '1.3', letterSpacing: '-0.02em' }],
        '4xl': ['36px', { lineHeight: '1.2', letterSpacing: '-0.02em' }],
      },
      boxShadow: {
        // Card shadows (Xiaomi style)
        'sm': '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
        'md': '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
        'lg': '0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2
```


## License: Apache-2.0
https://github.com/LexSwed/fxtrot-ui/blob/932303a8c8225b9587dd222c3d0f50f8a72d1529/src/lib/stitches.config.ts

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FADAI Blue
        primary: {
          DEFAULT: '#2B59FF',
          50: '#F0F4FF',
          100: '#E0E9FF',
          200: '#C1D3FF',
          300: '#A2BDFF',
          400: '#6B8EFF',
          500: '#2B59FF', // Main primary
          600: '#1F3FD6',
          700: '#1428A0',
          800: '#0E176D',
          900: '#080D3A',
        },
        // Background Off-white
        background: {
          DEFAULT: '#FBFBFB',
          light: '#F5F5F7',
        },
        // Text colors
        text: {
          primary: '#1D1D1F',
          secondary: '#86868B',
        },
      },
      fontFamily: {
        sans: ['Inter', ...defaultTheme.fontFamily.sans],
      },
      fontSize: {
        // Use tracking-tight pattern
        xs: ['12px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        sm: ['14px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        base: ['16px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        lg: ['18px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        xl: ['20px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        '2xl': ['24px', { lineHeight: '1.4', letterSpacing: '-0.02em' }],
        '3xl': ['30px', { lineHeight: '1.3', letterSpacing: '-0.02em' }],
        '4xl': ['36px', { lineHeight: '1.2', letterSpacing: '-0.02em' }],
      },
      boxShadow: {
        // Card shadows (Xiaomi style)
        'sm': '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
        'md': '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
        'lg': '0 10px 15px -3px rgba
```


## License: unknown
https://github.com/zrrd/spring-boot-learn/blob/ca0ed64ce93dd11f2d62bec15050c78833e9739b/spring-boot-27-amis/src/main/resources/static/amis/scss/_variables.scss

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FADAI Blue
        primary: {
          DEFAULT: '#2B59FF',
          50: '#F0F4FF',
          100: '#E0E9FF',
          200: '#C1D3FF',
          300: '#A2BDFF',
          400: '#6B8EFF',
          500: '#2B59FF', // Main primary
          600: '#1F3FD6',
          700: '#1428A0',
          800: '#0E176D',
          900: '#080D3A',
        },
        // Background Off-white
        background: {
          DEFAULT: '#FBFBFB',
          light: '#F5F5F7',
        },
        // Text colors
        text: {
          primary: '#1D1D1F',
          secondary: '#86868B',
        },
      },
      fontFamily: {
        sans: ['Inter', ...defaultTheme.fontFamily.sans],
      },
      fontSize: {
        // Use tracking-tight pattern
        xs: ['12px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        sm: ['14px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        base: ['16px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        lg: ['18px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        xl: ['20px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        '2xl': ['24px', { lineHeight: '1.4', letterSpacing: '-0.02em' }],
        '3xl': ['30px', { lineHeight: '1.3', letterSpacing: '-0.02em' }],
        '4xl': ['36px', { lineHeight: '1.2', letterSpacing: '-0.02em' }],
      },
      boxShadow: {
        // Card shadows (Xiaomi style)
        'sm': '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
        'md': '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
        'lg': '0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2
```


## License: Apache-2.0
https://github.com/LexSwed/fxtrot-ui/blob/932303a8c8225b9587dd222c3d0f50f8a72d1529/src/lib/stitches.config.ts

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FADAI Blue
        primary: {
          DEFAULT: '#2B59FF',
          50: '#F0F4FF',
          100: '#E0E9FF',
          200: '#C1D3FF',
          300: '#A2BDFF',
          400: '#6B8EFF',
          500: '#2B59FF', // Main primary
          600: '#1F3FD6',
          700: '#1428A0',
          800: '#0E176D',
          900: '#080D3A',
        },
        // Background Off-white
        background: {
          DEFAULT: '#FBFBFB',
          light: '#F5F5F7',
        },
        // Text colors
        text: {
          primary: '#1D1D1F',
          secondary: '#86868B',
        },
      },
      fontFamily: {
        sans: ['Inter', ...defaultTheme.fontFamily.sans],
      },
      fontSize: {
        // Use tracking-tight pattern
        xs: ['12px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        sm: ['14px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        base: ['16px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        lg: ['18px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        xl: ['20px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        '2xl': ['24px', { lineHeight: '1.4', letterSpacing: '-0.02em' }],
        '3xl': ['30px', { lineHeight: '1.3', letterSpacing: '-0.02em' }],
        '4xl': ['36px', { lineHeight: '1.2', letterSpacing: '-0.02em' }],
      },
      boxShadow: {
        // Card shadows (Xiaomi style)
        'sm': '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
        'md': '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
        'lg': '0 10px 15px -3px rgba
```


## License: unknown
https://github.com/zrrd/spring-boot-learn/blob/ca0ed64ce93dd11f2d62bec15050c78833e9739b/spring-boot-27-amis/src/main/resources/static/amis/scss/_variables.scss

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FADAI Blue
        primary: {
          DEFAULT: '#2B59FF',
          50: '#F0F4FF',
          100: '#E0E9FF',
          200: '#C1D3FF',
          300: '#A2BDFF',
          400: '#6B8EFF',
          500: '#2B59FF', // Main primary
          600: '#1F3FD6',
          700: '#1428A0',
          800: '#0E176D',
          900: '#080D3A',
        },
        // Background Off-white
        background: {
          DEFAULT: '#FBFBFB',
          light: '#F5F5F7',
        },
        // Text colors
        text: {
          primary: '#1D1D1F',
          secondary: '#86868B',
        },
      },
      fontFamily: {
        sans: ['Inter', ...defaultTheme.fontFamily.sans],
      },
      fontSize: {
        // Use tracking-tight pattern
        xs: ['12px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        sm: ['14px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        base: ['16px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        lg: ['18px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        xl: ['20px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        '2xl': ['24px', { lineHeight: '1.4', letterSpacing: '-0.02em' }],
        '3xl': ['30px', { lineHeight: '1.3', letterSpacing: '-0.02em' }],
        '4xl': ['36px', { lineHeight: '1.2', letterSpacing: '-0.02em' }],
      },
      boxShadow: {
        // Card shadows (Xiaomi style)
        'sm': '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
        'md': '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
        'lg': '0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2
```


## License: Apache-2.0
https://github.com/LexSwed/fxtrot-ui/blob/932303a8c8225b9587dd222c3d0f50f8a72d1529/src/lib/stitches.config.ts

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FADAI Blue
        primary: {
          DEFAULT: '#2B59FF',
          50: '#F0F4FF',
          100: '#E0E9FF',
          200: '#C1D3FF',
          300: '#A2BDFF',
          400: '#6B8EFF',
          500: '#2B59FF', // Main primary
          600: '#1F3FD6',
          700: '#1428A0',
          800: '#0E176D',
          900: '#080D3A',
        },
        // Background Off-white
        background: {
          DEFAULT: '#FBFBFB',
          light: '#F5F5F7',
        },
        // Text colors
        text: {
          primary: '#1D1D1F',
          secondary: '#86868B',
        },
      },
      fontFamily: {
        sans: ['Inter', ...defaultTheme.fontFamily.sans],
      },
      fontSize: {
        // Use tracking-tight pattern
        xs: ['12px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        sm: ['14px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        base: ['16px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        lg: ['18px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        xl: ['20px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        '2xl': ['24px', { lineHeight: '1.4', letterSpacing: '-0.02em' }],
        '3xl': ['30px', { lineHeight: '1.3', letterSpacing: '-0.02em' }],
        '4xl': ['36px', { lineHeight: '1.2', letterSpacing: '-0.02em' }],
      },
      boxShadow: {
        // Card shadows (Xiaomi style)
        'sm': '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
        'md': '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
        'lg': '0 10px 15px -3px rgba
```


## License: unknown
https://github.com/zrrd/spring-boot-learn/blob/ca0ed64ce93dd11f2d62bec15050c78833e9739b/spring-boot-27-amis/src/main/resources/static/amis/scss/_variables.scss

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FADAI Blue
        primary: {
          DEFAULT: '#2B59FF',
          50: '#F0F4FF',
          100: '#E0E9FF',
          200: '#C1D3FF',
          300: '#A2BDFF',
          400: '#6B8EFF',
          500: '#2B59FF', // Main primary
          600: '#1F3FD6',
          700: '#1428A0',
          800: '#0E176D',
          900: '#080D3A',
        },
        // Background Off-white
        background: {
          DEFAULT: '#FBFBFB',
          light: '#F5F5F7',
        },
        // Text colors
        text: {
          primary: '#1D1D1F',
          secondary: '#86868B',
        },
      },
      fontFamily: {
        sans: ['Inter', ...defaultTheme.fontFamily.sans],
      },
      fontSize: {
        // Use tracking-tight pattern
        xs: ['12px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        sm: ['14px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        base: ['16px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        lg: ['18px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        xl: ['20px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        '2xl': ['24px', { lineHeight: '1.4', letterSpacing: '-0.02em' }],
        '3xl': ['30px', { lineHeight: '1.3', letterSpacing: '-0.02em' }],
        '4xl': ['36px', { lineHeight: '1.2', letterSpacing: '-0.02em' }],
      },
      boxShadow: {
        // Card shadows (Xiaomi style)
        'sm': '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
        'md': '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
        'lg': '0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2
```


## License: Apache-2.0
https://github.com/LexSwed/fxtrot-ui/blob/932303a8c8225b9587dd222c3d0f50f8a72d1529/src/lib/stitches.config.ts

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FADAI Blue
        primary: {
          DEFAULT: '#2B59FF',
          50: '#F0F4FF',
          100: '#E0E9FF',
          200: '#C1D3FF',
          300: '#A2BDFF',
          400: '#6B8EFF',
          500: '#2B59FF', // Main primary
          600: '#1F3FD6',
          700: '#1428A0',
          800: '#0E176D',
          900: '#080D3A',
        },
        // Background Off-white
        background: {
          DEFAULT: '#FBFBFB',
          light: '#F5F5F7',
        },
        // Text colors
        text: {
          primary: '#1D1D1F',
          secondary: '#86868B',
        },
      },
      fontFamily: {
        sans: ['Inter', ...defaultTheme.fontFamily.sans],
      },
      fontSize: {
        // Use tracking-tight pattern
        xs: ['12px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        sm: ['14px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        base: ['16px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        lg: ['18px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        xl: ['20px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        '2xl': ['24px', { lineHeight: '1.4', letterSpacing: '-0.02em' }],
        '3xl': ['30px', { lineHeight: '1.3', letterSpacing: '-0.02em' }],
        '4xl': ['36px', { lineHeight: '1.2', letterSpacing: '-0.02em' }],
      },
      boxShadow: {
        // Card shadows (Xiaomi style)
        'sm': '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
        'md': '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
        'lg': '0 10px 15px -3px rgba
```


## License: unknown
https://github.com/zrrd/spring-boot-learn/blob/ca0ed64ce93dd11f2d62bec15050c78833e9739b/spring-boot-27-amis/src/main/resources/static/amis/scss/_variables.scss

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FADAI Blue
        primary: {
          DEFAULT: '#2B59FF',
          50: '#F0F4FF',
          100: '#E0E9FF',
          200: '#C1D3FF',
          300: '#A2BDFF',
          400: '#6B8EFF',
          500: '#2B59FF', // Main primary
          600: '#1F3FD6',
          700: '#1428A0',
          800: '#0E176D',
          900: '#080D3A',
        },
        // Background Off-white
        background: {
          DEFAULT: '#FBFBFB',
          light: '#F5F5F7',
        },
        // Text colors
        text: {
          primary: '#1D1D1F',
          secondary: '#86868B',
        },
      },
      fontFamily: {
        sans: ['Inter', ...defaultTheme.fontFamily.sans],
      },
      fontSize: {
        // Use tracking-tight pattern
        xs: ['12px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        sm: ['14px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        base: ['16px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        lg: ['18px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        xl: ['20px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        '2xl': ['24px', { lineHeight: '1.4', letterSpacing: '-0.02em' }],
        '3xl': ['30px', { lineHeight: '1.3', letterSpacing: '-0.02em' }],
        '4xl': ['36px', { lineHeight: '1.2', letterSpacing: '-0.02em' }],
      },
      boxShadow: {
        // Card shadows (Xiaomi style)
        'sm': '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
        'md': '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
        'lg': '0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2
```


## License: unknown
https://github.com/zrrd/spring-boot-learn/blob/ca0ed64ce93dd11f2d62bec15050c78833e9739b/spring-boot-27-amis/src/main/resources/static/amis/scss/_variables.scss

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FADAI Blue
        primary: {
          DEFAULT: '#2B59FF',
          50: '#F0F4FF',
          100: '#E0E9FF',
          200: '#C1D3FF',
          300: '#A2BDFF',
          400: '#6B8EFF',
          500: '#2B59FF', // Main primary
          600: '#1F3FD6',
          700: '#1428A0',
          800: '#0E176D',
          900: '#080D3A',
        },
        // Background Off-white
        background: {
          DEFAULT: '#FBFBFB',
          light: '#F5F5F7',
        },
        // Text colors
        text: {
          primary: '#1D1D1F',
          secondary: '#86868B',
        },
      },
      fontFamily: {
        sans: ['Inter', ...defaultTheme.fontFamily.sans],
      },
      fontSize: {
        // Use tracking-tight pattern
        xs: ['12px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        sm: ['14px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        base: ['16px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        lg: ['18px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        xl: ['20px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        '2xl': ['24px', { lineHeight: '1.4', letterSpacing: '-0.02em' }],
        '3xl': ['30px', { lineHeight: '1.3', letterSpacing: '-0.02em' }],
        '4xl': ['36px', { lineHeight: '1.2', letterSpacing: '-0.02em' }],
      },
      boxShadow: {
        // Card shadows (Xiaomi style)
        'sm': '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
        'md': '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
        'lg': '0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2
```


## License: Apache-2.0
https://github.com/LexSwed/fxtrot-ui/blob/932303a8c8225b9587dd222c3d0f50f8a72d1529/src/lib/stitches.config.ts

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FADAI Blue
        primary: {
          DEFAULT: '#2B59FF',
          50: '#F0F4FF',
          100: '#E0E9FF',
          200: '#C1D3FF',
          300: '#A2BDFF',
          400: '#6B8EFF',
          500: '#2B59FF', // Main primary
          600: '#1F3FD6',
          700: '#1428A0',
          800: '#0E176D',
          900: '#080D3A',
        },
        // Background Off-white
        background: {
          DEFAULT: '#FBFBFB',
          light: '#F5F5F7',
        },
        // Text colors
        text: {
          primary: '#1D1D1F',
          secondary: '#86868B',
        },
      },
      fontFamily: {
        sans: ['Inter', ...defaultTheme.fontFamily.sans],
      },
      fontSize: {
        // Use tracking-tight pattern
        xs: ['12px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        sm: ['14px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        base: ['16px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        lg: ['18px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        xl: ['20px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        '2xl': ['24px', { lineHeight: '1.4', letterSpacing: '-0.02em' }],
        '3xl': ['30px', { lineHeight: '1.3', letterSpacing: '-0.02em' }],
        '4xl': ['36px', { lineHeight: '1.2', letterSpacing: '-0.02em' }],
      },
      boxShadow: {
        // Card shadows (Xiaomi style)
        'sm': '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
        'md': '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
        'lg': '0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2
```


## License: unknown
https://github.com/zrrd/spring-boot-learn/blob/ca0ed64ce93dd11f2d62bec15050c78833e9739b/spring-boot-27-amis/src/main/resources/static/amis/scss/_variables.scss

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FADAI Blue
        primary: {
          DEFAULT: '#2B59FF',
          50: '#F0F4FF',
          100: '#E0E9FF',
          200: '#C1D3FF',
          300: '#A2BDFF',
          400: '#6B8EFF',
          500: '#2B59FF', // Main primary
          600: '#1F3FD6',
          700: '#1428A0',
          800: '#0E176D',
          900: '#080D3A',
        },
        // Background Off-white
        background: {
          DEFAULT: '#FBFBFB',
          light: '#F5F5F7',
        },
        // Text colors
        text: {
          primary: '#1D1D1F',
          secondary: '#86868B',
        },
      },
      fontFamily: {
        sans: ['Inter', ...defaultTheme.fontFamily.sans],
      },
      fontSize: {
        // Use tracking-tight pattern
        xs: ['12px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        sm: ['14px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        base: ['16px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        lg: ['18px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        xl: ['20px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        '2xl': ['24px', { lineHeight: '1.4', letterSpacing: '-0.02em' }],
        '3xl': ['30px', { lineHeight: '1.3', letterSpacing: '-0.02em' }],
        '4xl': ['36px', { lineHeight: '1.2', letterSpacing: '-0.02em' }],
      },
      boxShadow: {
        // Card shadows (Xiaomi style)
        'sm': '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
        'md': '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
        'lg': '0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2
```


## License: Apache-2.0
https://github.com/LexSwed/fxtrot-ui/blob/932303a8c8225b9587dd222c3d0f50f8a72d1529/src/lib/stitches.config.ts

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FADAI Blue
        primary: {
          DEFAULT: '#2B59FF',
          50: '#F0F4FF',
          100: '#E0E9FF',
          200: '#C1D3FF',
          300: '#A2BDFF',
          400: '#6B8EFF',
          500: '#2B59FF', // Main primary
          600: '#1F3FD6',
          700: '#1428A0',
          800: '#0E176D',
          900: '#080D3A',
        },
        // Background Off-white
        background: {
          DEFAULT: '#FBFBFB',
          light: '#F5F5F7',
        },
        // Text colors
        text: {
          primary: '#1D1D1F',
          secondary: '#86868B',
        },
      },
      fontFamily: {
        sans: ['Inter', ...defaultTheme.fontFamily.sans],
      },
      fontSize: {
        // Use tracking-tight pattern
        xs: ['12px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        sm: ['14px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        base: ['16px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        lg: ['18px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        xl: ['20px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        '2xl': ['24px', { lineHeight: '1.4', letterSpacing: '-0.02em' }],
        '3xl': ['30px', { lineHeight: '1.3', letterSpacing: '-0.02em' }],
        '4xl': ['36px', { lineHeight: '1.2', letterSpacing: '-0.02em' }],
      },
      boxShadow: {
        // Card shadows (Xiaomi style)
        'sm': '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
        'md': '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
        'lg': '0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2
```


## License: unknown
https://github.com/zrrd/spring-boot-learn/blob/ca0ed64ce93dd11f2d62bec15050c78833e9739b/spring-boot-27-amis/src/main/resources/static/amis/scss/_variables.scss

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FADAI Blue
        primary: {
          DEFAULT: '#2B59FF',
          50: '#F0F4FF',
          100: '#E0E9FF',
          200: '#C1D3FF',
          300: '#A2BDFF',
          400: '#6B8EFF',
          500: '#2B59FF', // Main primary
          600: '#1F3FD6',
          700: '#1428A0',
          800: '#0E176D',
          900: '#080D3A',
        },
        // Background Off-white
        background: {
          DEFAULT: '#FBFBFB',
          light: '#F5F5F7',
        },
        // Text colors
        text: {
          primary: '#1D1D1F',
          secondary: '#86868B',
        },
      },
      fontFamily: {
        sans: ['Inter', ...defaultTheme.fontFamily.sans],
      },
      fontSize: {
        // Use tracking-tight pattern
        xs: ['12px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        sm: ['14px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        base: ['16px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        lg: ['18px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        xl: ['20px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        '2xl': ['24px', { lineHeight: '1.4', letterSpacing: '-0.02em' }],
        '3xl': ['30px', { lineHeight: '1.3', letterSpacing: '-0.02em' }],
        '4xl': ['36px', { lineHeight: '1.2', letterSpacing: '-0.02em' }],
      },
      boxShadow: {
        // Card shadows (Xiaomi style)
        'sm': '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
        'md': '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
        'lg': '0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2
```


## License: Apache-2.0
https://github.com/LexSwed/fxtrot-ui/blob/932303a8c8225b9587dd222c3d0f50f8a72d1529/src/lib/stitches.config.ts

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FADAI Blue
        primary: {
          DEFAULT: '#2B59FF',
          50: '#F0F4FF',
          100: '#E0E9FF',
          200: '#C1D3FF',
          300: '#A2BDFF',
          400: '#6B8EFF',
          500: '#2B59FF', // Main primary
          600: '#1F3FD6',
          700: '#1428A0',
          800: '#0E176D',
          900: '#080D3A',
        },
        // Background Off-white
        background: {
          DEFAULT: '#FBFBFB',
          light: '#F5F5F7',
        },
        // Text colors
        text: {
          primary: '#1D1D1F',
          secondary: '#86868B',
        },
      },
      fontFamily: {
        sans: ['Inter', ...defaultTheme.fontFamily.sans],
      },
      fontSize: {
        // Use tracking-tight pattern
        xs: ['12px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        sm: ['14px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        base: ['16px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        lg: ['18px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        xl: ['20px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        '2xl': ['24px', { lineHeight: '1.4', letterSpacing: '-0.02em' }],
        '3xl': ['30px', { lineHeight: '1.3', letterSpacing: '-0.02em' }],
        '4xl': ['36px', { lineHeight: '1.2', letterSpacing: '-0.02em' }],
      },
      boxShadow: {
        // Card shadows (Xiaomi style)
        'sm': '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
        'md': '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
        'lg': '0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2
```


## License: unknown
https://github.com/zrrd/spring-boot-learn/blob/ca0ed64ce93dd11f2d62bec15050c78833e9739b/spring-boot-27-amis/src/main/resources/static/amis/scss/_variables.scss

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FADAI Blue
        primary: {
          DEFAULT: '#2B59FF',
          50: '#F0F4FF',
          100: '#E0E9FF',
          200: '#C1D3FF',
          300: '#A2BDFF',
          400: '#6B8EFF',
          500: '#2B59FF', // Main primary
          600: '#1F3FD6',
          700: '#1428A0',
          800: '#0E176D',
          900: '#080D3A',
        },
        // Background Off-white
        background: {
          DEFAULT: '#FBFBFB',
          light: '#F5F5F7',
        },
        // Text colors
        text: {
          primary: '#1D1D1F',
          secondary: '#86868B',
        },
      },
      fontFamily: {
        sans: ['Inter', ...defaultTheme.fontFamily.sans],
      },
      fontSize: {
        // Use tracking-tight pattern
        xs: ['12px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        sm: ['14px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        base: ['16px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        lg: ['18px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        xl: ['20px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        '2xl': ['24px', { lineHeight: '1.4', letterSpacing: '-0.02em' }],
        '3xl': ['30px', { lineHeight: '1.3', letterSpacing: '-0.02em' }],
        '4xl': ['36px', { lineHeight: '1.2', letterSpacing: '-0.02em' }],
      },
      boxShadow: {
        // Card shadows (Xiaomi style)
        'sm': '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
        'md': '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
        'lg': '0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05)',
        //
```


## License: Apache-2.0
https://github.com/LexSwed/fxtrot-ui/blob/932303a8c8225b9587dd222c3d0f50f8a72d1529/src/lib/stitches.config.ts

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FADAI Blue
        primary: {
          DEFAULT: '#2B59FF',
          50: '#F0F4FF',
          100: '#E0E9FF',
          200: '#C1D3FF',
          300: '#A2BDFF',
          400: '#6B8EFF',
          500: '#2B59FF', // Main primary
          600: '#1F3FD6',
          700: '#1428A0',
          800: '#0E176D',
          900: '#080D3A',
        },
        // Background Off-white
        background: {
          DEFAULT: '#FBFBFB',
          light: '#F5F5F7',
        },
        // Text colors
        text: {
          primary: '#1D1D1F',
          secondary: '#86868B',
        },
      },
      fontFamily: {
        sans: ['Inter', ...defaultTheme.fontFamily.sans],
      },
      fontSize: {
        // Use tracking-tight pattern
        xs: ['12px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        sm: ['14px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        base: ['16px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        lg: ['18px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        xl: ['20px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        '2xl': ['24px', { lineHeight: '1.4', letterSpacing: '-0.02em' }],
        '3xl': ['30px', { lineHeight: '1.3', letterSpacing: '-0.02em' }],
        '4xl': ['36px', { lineHeight: '1.2', letterSpacing: '-0.02em' }],
      },
      boxShadow: {
        // Card shadows (Xiaomi style)
        'sm': '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
        'md': '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
        'lg': '0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05)',
        //
```


## License: unknown
https://github.com/zrrd/spring-boot-learn/blob/ca0ed64ce93dd11f2d62bec15050c78833e9739b/spring-boot-27-amis/src/main/resources/static/amis/scss/_variables.scss

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FADAI Blue
        primary: {
          DEFAULT: '#2B59FF',
          50: '#F0F4FF',
          100: '#E0E9FF',
          200: '#C1D3FF',
          300: '#A2BDFF',
          400: '#6B8EFF',
          500: '#2B59FF', // Main primary
          600: '#1F3FD6',
          700: '#1428A0',
          800: '#0E176D',
          900: '#080D3A',
        },
        // Background Off-white
        background: {
          DEFAULT: '#FBFBFB',
          light: '#F5F5F7',
        },
        // Text colors
        text: {
          primary: '#1D1D1F',
          secondary: '#86868B',
        },
      },
      fontFamily: {
        sans: ['Inter', ...defaultTheme.fontFamily.sans],
      },
      fontSize: {
        // Use tracking-tight pattern
        xs: ['12px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        sm: ['14px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        base: ['16px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        lg: ['18px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        xl: ['20px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        '2xl': ['24px', { lineHeight: '1.4', letterSpacing: '-0.02em' }],
        '3xl': ['30px', { lineHeight: '1.3', letterSpacing: '-0.02em' }],
        '4xl': ['36px', { lineHeight: '1.2', letterSpacing: '-0.02em' }],
      },
      boxShadow: {
        // Card shadows (Xiaomi style)
        'sm': '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
        'md': '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
        'lg': '0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05)',
        //
```


## License: Apache-2.0
https://github.com/LexSwed/fxtrot-ui/blob/932303a8c8225b9587dd222c3d0f50f8a72d1529/src/lib/stitches.config.ts

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FADAI Blue
        primary: {
          DEFAULT: '#2B59FF',
          50: '#F0F4FF',
          100: '#E0E9FF',
          200: '#C1D3FF',
          300: '#A2BDFF',
          400: '#6B8EFF',
          500: '#2B59FF', // Main primary
          600: '#1F3FD6',
          700: '#1428A0',
          800: '#0E176D',
          900: '#080D3A',
        },
        // Background Off-white
        background: {
          DEFAULT: '#FBFBFB',
          light: '#F5F5F7',
        },
        // Text colors
        text: {
          primary: '#1D1D1F',
          secondary: '#86868B',
        },
      },
      fontFamily: {
        sans: ['Inter', ...defaultTheme.fontFamily.sans],
      },
      fontSize: {
        // Use tracking-tight pattern
        xs: ['12px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        sm: ['14px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        base: ['16px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        lg: ['18px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        xl: ['20px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        '2xl': ['24px', { lineHeight: '1.4', letterSpacing: '-0.02em' }],
        '3xl': ['30px', { lineHeight: '1.3', letterSpacing: '-0.02em' }],
        '4xl': ['36px', { lineHeight: '1.2', letterSpacing: '-0.02em' }],
      },
      boxShadow: {
        // Card shadows (Xiaomi style)
        'sm': '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
        'md': '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
        'lg': '0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05)',
        //
```


## License: unknown
https://github.com/zrrd/spring-boot-learn/blob/ca0ed64ce93dd11f2d62bec15050c78833e9739b/spring-boot-27-amis/src/main/resources/static/amis/scss/_variables.scss

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FADAI Blue
        primary: {
          DEFAULT: '#2B59FF',
          50: '#F0F4FF',
          100: '#E0E9FF',
          200: '#C1D3FF',
          300: '#A2BDFF',
          400: '#6B8EFF',
          500: '#2B59FF', // Main primary
          600: '#1F3FD6',
          700: '#1428A0',
          800: '#0E176D',
          900: '#080D3A',
        },
        // Background Off-white
        background: {
          DEFAULT: '#FBFBFB',
          light: '#F5F5F7',
        },
        // Text colors
        text: {
          primary: '#1D1D1F',
          secondary: '#86868B',
        },
      },
      fontFamily: {
        sans: ['Inter', ...defaultTheme.fontFamily.sans],
      },
      fontSize: {
        // Use tracking-tight pattern
        xs: ['12px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        sm: ['14px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        base: ['16px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        lg: ['18px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        xl: ['20px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        '2xl': ['24px', { lineHeight: '1.4', letterSpacing: '-0.02em' }],
        '3xl': ['30px', { lineHeight: '1.3', letterSpacing: '-0.02em' }],
        '4xl': ['36px', { lineHeight: '1.2', letterSpacing: '-0.02em' }],
      },
      boxShadow: {
        // Card shadows (Xiaomi style)
        'sm': '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
        'md': '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
        'lg': '0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05)',
        // Soft car
```


## License: Apache-2.0
https://github.com/LexSwed/fxtrot-ui/blob/932303a8c8225b9587dd222c3d0f50f8a72d1529/src/lib/stitches.config.ts

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FADAI Blue
        primary: {
          DEFAULT: '#2B59FF',
          50: '#F0F4FF',
          100: '#E0E9FF',
          200: '#C1D3FF',
          300: '#A2BDFF',
          400: '#6B8EFF',
          500: '#2B59FF', // Main primary
          600: '#1F3FD6',
          700: '#1428A0',
          800: '#0E176D',
          900: '#080D3A',
        },
        // Background Off-white
        background: {
          DEFAULT: '#FBFBFB',
          light: '#F5F5F7',
        },
        // Text colors
        text: {
          primary: '#1D1D1F',
          secondary: '#86868B',
        },
      },
      fontFamily: {
        sans: ['Inter', ...defaultTheme.fontFamily.sans],
      },
      fontSize: {
        // Use tracking-tight pattern
        xs: ['12px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        sm: ['14px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        base: ['16px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        lg: ['18px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        xl: ['20px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        '2xl': ['24px', { lineHeight: '1.4', letterSpacing: '-0.02em' }],
        '3xl': ['30px', { lineHeight: '1.3', letterSpacing: '-0.02em' }],
        '4xl': ['36px', { lineHeight: '1.2', letterSpacing: '-0.02em' }],
      },
      boxShadow: {
        // Card shadows (Xiaomi style)
        'sm': '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
        'md': '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
        'lg': '0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05)',
        // Soft car
```


## License: unknown
https://github.com/zrrd/spring-boot-learn/blob/ca0ed64ce93dd11f2d62bec15050c78833e9739b/spring-boot-27-amis/src/main/resources/static/amis/scss/_variables.scss

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FADAI Blue
        primary: {
          DEFAULT: '#2B59FF',
          50: '#F0F4FF',
          100: '#E0E9FF',
          200: '#C1D3FF',
          300: '#A2BDFF',
          400: '#6B8EFF',
          500: '#2B59FF', // Main primary
          600: '#1F3FD6',
          700: '#1428A0',
          800: '#0E176D',
          900: '#080D3A',
        },
        // Background Off-white
        background: {
          DEFAULT: '#FBFBFB',
          light: '#F5F5F7',
        },
        // Text colors
        text: {
          primary: '#1D1D1F',
          secondary: '#86868B',
        },
      },
      fontFamily: {
        sans: ['Inter', ...defaultTheme.fontFamily.sans],
      },
      fontSize: {
        // Use tracking-tight pattern
        xs: ['12px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        sm: ['14px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        base: ['16px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        lg: ['18px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        xl: ['20px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        '2xl': ['24px', { lineHeight: '1.4', letterSpacing: '-0.02em' }],
        '3xl': ['30px', { lineHeight: '1.3', letterSpacing: '-0.02em' }],
        '4xl': ['36px', { lineHeight: '1.2', letterSpacing: '-0.02em' }],
      },
      boxShadow: {
        // Card shadows (Xiaomi style)
        'sm': '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
        'md': '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
        'lg': '0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05)',
        // Soft car
```


## License: Apache-2.0
https://github.com/LexSwed/fxtrot-ui/blob/932303a8c8225b9587dd222c3d0f50f8a72d1529/src/lib/stitches.config.ts

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FADAI Blue
        primary: {
          DEFAULT: '#2B59FF',
          50: '#F0F4FF',
          100: '#E0E9FF',
          200: '#C1D3FF',
          300: '#A2BDFF',
          400: '#6B8EFF',
          500: '#2B59FF', // Main primary
          600: '#1F3FD6',
          700: '#1428A0',
          800: '#0E176D',
          900: '#080D3A',
        },
        // Background Off-white
        background: {
          DEFAULT: '#FBFBFB',
          light: '#F5F5F7',
        },
        // Text colors
        text: {
          primary: '#1D1D1F',
          secondary: '#86868B',
        },
      },
      fontFamily: {
        sans: ['Inter', ...defaultTheme.fontFamily.sans],
      },
      fontSize: {
        // Use tracking-tight pattern
        xs: ['12px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        sm: ['14px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        base: ['16px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        lg: ['18px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        xl: ['20px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        '2xl': ['24px', { lineHeight: '1.4', letterSpacing: '-0.02em' }],
        '3xl': ['30px', { lineHeight: '1.3', letterSpacing: '-0.02em' }],
        '4xl': ['36px', { lineHeight: '1.2', letterSpacing: '-0.02em' }],
      },
      boxShadow: {
        // Card shadows (Xiaomi style)
        'sm': '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
        'md': '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
        'lg': '0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05)',
        // Soft car
```


## License: MIT
https://github.com/bumang/movie-app/blob/a27ebfd53beecc3a23da2414e7fe00acc5d814ec/tailwind.config.ts

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FADAI Blue
        primary: {
          DEFAULT: '#2B59FF',
          50: '#F0F4FF',
          100: '#E0E9FF',
          200: '#C1D3FF',
          300: '#A2BDFF',
          400: '#6B8EFF',
          500: '#2B59FF', // Main primary
          600: '#1F3FD6',
          700: '#1428A0',
          800: '#0E176D',
          900: '#080D3A',
        },
        // Background Off-white
        background: {
          DEFAULT: '#FBFBFB',
          light: '#F5F5F7',
        },
        // Text colors
        text: {
          primary: '#1D1D1F',
          secondary: '#86868B',
        },
      },
      fontFamily: {
        sans: ['Inter', ...defaultTheme.fontFamily.sans],
      },
      fontSize: {
        // Use tracking-tight pattern
        xs: ['12px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        sm: ['14px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        base: ['16px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        lg: ['18px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        xl: ['20px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        '2xl': ['24px', { lineHeight: '1.4', letterSpacing: '-0.02em' }],
        '3xl': ['30px', { lineHeight: '1.3', letterSpacing: '-0.02em' }],
        '4xl': ['36px', { lineHeight: '1.2', letterSpacing: '-0.02em' }],
      },
      boxShadow: {
        // Card shadows (Xiaomi style)
        'sm': '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
        'md': '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
        'lg': '0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05)',
        // Soft card shadow
        'card': '0 2px 8px rgba(0, 0, 0, 0.08)',
        // Elevated soft shadow
        'card-lg': '0 8px 16px rgba(0, 0, 0, 0.12)',
      },
      borderRadius: {
        'card': '16px', // Xiaomi card style
        '2xl': '16px',
      },
      animation: {
        'fade-in': 'fadeIn 0.6s ease-out',
      },
      keyframes: {
        fadeIn: {
          '0%': { opacity: '0', transform: 'translateY(20px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
      },
    },
  },
  plugins: [],
};
```


## License: MIT
https://github.com/bumang/movie-app/blob/a27ebfd53beecc3a23da2414e7fe00acc5d814ec/tailwind.config.ts

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FADAI Blue
        primary: {
          DEFAULT: '#2B59FF',
          50: '#F0F4FF',
          100: '#E0E9FF',
          200: '#C1D3FF',
          300: '#A2BDFF',
          400: '#6B8EFF',
          500: '#2B59FF', // Main primary
          600: '#1F3FD6',
          700: '#1428A0',
          800: '#0E176D',
          900: '#080D3A',
        },
        // Background Off-white
        background: {
          DEFAULT: '#FBFBFB',
          light: '#F5F5F7',
        },
        // Text colors
        text: {
          primary: '#1D1D1F',
          secondary: '#86868B',
        },
      },
      fontFamily: {
        sans: ['Inter', ...defaultTheme.fontFamily.sans],
      },
      fontSize: {
        // Use tracking-tight pattern
        xs: ['12px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        sm: ['14px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        base: ['16px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        lg: ['18px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        xl: ['20px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        '2xl': ['24px', { lineHeight: '1.4', letterSpacing: '-0.02em' }],
        '3xl': ['30px', { lineHeight: '1.3', letterSpacing: '-0.02em' }],
        '4xl': ['36px', { lineHeight: '1.2', letterSpacing: '-0.02em' }],
      },
      boxShadow: {
        // Card shadows (Xiaomi style)
        'sm': '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
        'md': '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
        'lg': '0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05)',
        // Soft card shadow
        'card': '0 2px 8px rgba(0, 0, 0, 0.08)',
        // Elevated soft shadow
        'card-lg': '0 8px 16px rgba(0, 0, 0, 0.12)',
      },
      borderRadius: {
        'card': '16px', // Xiaomi card style
        '2xl': '16px',
      },
      animation: {
        'fade-in': 'fadeIn 0.6s ease-out',
      },
      keyframes: {
        fadeIn: {
          '0%': { opacity: '0', transform: 'translateY(20px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
      },
    },
  },
  plugins: [],
};
```


## License: MIT
https://github.com/bumang/movie-app/blob/a27ebfd53beecc3a23da2414e7fe00acc5d814ec/tailwind.config.ts

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FADAI Blue
        primary: {
          DEFAULT: '#2B59FF',
          50: '#F0F4FF',
          100: '#E0E9FF',
          200: '#C1D3FF',
          300: '#A2BDFF',
          400: '#6B8EFF',
          500: '#2B59FF', // Main primary
          600: '#1F3FD6',
          700: '#1428A0',
          800: '#0E176D',
          900: '#080D3A',
        },
        // Background Off-white
        background: {
          DEFAULT: '#FBFBFB',
          light: '#F5F5F7',
        },
        // Text colors
        text: {
          primary: '#1D1D1F',
          secondary: '#86868B',
        },
      },
      fontFamily: {
        sans: ['Inter', ...defaultTheme.fontFamily.sans],
      },
      fontSize: {
        // Use tracking-tight pattern
        xs: ['12px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        sm: ['14px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        base: ['16px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        lg: ['18px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        xl: ['20px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        '2xl': ['24px', { lineHeight: '1.4', letterSpacing: '-0.02em' }],
        '3xl': ['30px', { lineHeight: '1.3', letterSpacing: '-0.02em' }],
        '4xl': ['36px', { lineHeight: '1.2', letterSpacing: '-0.02em' }],
      },
      boxShadow: {
        // Card shadows (Xiaomi style)
        'sm': '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
        'md': '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
        'lg': '0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05)',
        // Soft card shadow
        'card': '0 2px 8px rgba(0, 0, 0, 0.08)',
        // Elevated soft shadow
        'card-lg': '0 8px 16px rgba(0, 0, 0, 0.12)',
      },
      borderRadius: {
        'card': '16px', // Xiaomi card style
        '2xl': '16px',
      },
      animation: {
        'fade-in': 'fadeIn 0.6s ease-out',
      },
      keyframes: {
        fadeIn: {
          '0%': { opacity: '0', transform: 'translateY(20px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
      },
    },
  },
  plugins: [],
};
```


## License: MIT
https://github.com/bumang/movie-app/blob/a27ebfd53beecc3a23da2414e7fe00acc5d814ec/tailwind.config.ts

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FADAI Blue
        primary: {
          DEFAULT: '#2B59FF',
          50: '#F0F4FF',
          100: '#E0E9FF',
          200: '#C1D3FF',
          300: '#A2BDFF',
          400: '#6B8EFF',
          500: '#2B59FF', // Main primary
          600: '#1F3FD6',
          700: '#1428A0',
          800: '#0E176D',
          900: '#080D3A',
        },
        // Background Off-white
        background: {
          DEFAULT: '#FBFBFB',
          light: '#F5F5F7',
        },
        // Text colors
        text: {
          primary: '#1D1D1F',
          secondary: '#86868B',
        },
      },
      fontFamily: {
        sans: ['Inter', ...defaultTheme.fontFamily.sans],
      },
      fontSize: {
        // Use tracking-tight pattern
        xs: ['12px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        sm: ['14px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        base: ['16px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        lg: ['18px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        xl: ['20px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        '2xl': ['24px', { lineHeight: '1.4', letterSpacing: '-0.02em' }],
        '3xl': ['30px', { lineHeight: '1.3', letterSpacing: '-0.02em' }],
        '4xl': ['36px', { lineHeight: '1.2', letterSpacing: '-0.02em' }],
      },
      boxShadow: {
        // Card shadows (Xiaomi style)
        'sm': '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
        'md': '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
        'lg': '0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05)',
        // Soft card shadow
        'card': '0 2px 8px rgba(0, 0, 0, 0.08)',
        // Elevated soft shadow
        'card-lg': '0 8px 16px rgba(0, 0, 0, 0.12)',
      },
      borderRadius: {
        'card': '16px', // Xiaomi card style
        '2xl': '16px',
      },
      animation: {
        'fade-in': 'fadeIn 0.6s ease-out',
      },
      keyframes: {
        fadeIn: {
          '0%': { opacity: '0', transform: 'translateY(20px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
      },
    },
  },
  plugins: [],
};
```


## License: MIT
https://github.com/bumang/movie-app/blob/a27ebfd53beecc3a23da2414e7fe00acc5d814ec/tailwind.config.ts

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FADAI Blue
        primary: {
          DEFAULT: '#2B59FF',
          50: '#F0F4FF',
          100: '#E0E9FF',
          200: '#C1D3FF',
          300: '#A2BDFF',
          400: '#6B8EFF',
          500: '#2B59FF', // Main primary
          600: '#1F3FD6',
          700: '#1428A0',
          800: '#0E176D',
          900: '#080D3A',
        },
        // Background Off-white
        background: {
          DEFAULT: '#FBFBFB',
          light: '#F5F5F7',
        },
        // Text colors
        text: {
          primary: '#1D1D1F',
          secondary: '#86868B',
        },
      },
      fontFamily: {
        sans: ['Inter', ...defaultTheme.fontFamily.sans],
      },
      fontSize: {
        // Use tracking-tight pattern
        xs: ['12px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        sm: ['14px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        base: ['16px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        lg: ['18px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        xl: ['20px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        '2xl': ['24px', { lineHeight: '1.4', letterSpacing: '-0.02em' }],
        '3xl': ['30px', { lineHeight: '1.3', letterSpacing: '-0.02em' }],
        '4xl': ['36px', { lineHeight: '1.2', letterSpacing: '-0.02em' }],
      },
      boxShadow: {
        // Card shadows (Xiaomi style)
        'sm': '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
        'md': '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
        'lg': '0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05)',
        // Soft card shadow
        'card': '0 2px 8px rgba(0, 0, 0, 0.08)',
        // Elevated soft shadow
        'card-lg': '0 8px 16px rgba(0, 0, 0, 0.12)',
      },
      borderRadius: {
        'card': '16px', // Xiaomi card style
        '2xl': '16px',
      },
      animation: {
        'fade-in': 'fadeIn 0.6s ease-out',
      },
      keyframes: {
        fadeIn: {
          '0%': { opacity: '0', transform: 'translateY(20px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
      },
    },
  },
  plugins: [],
};
```


## License: MIT
https://github.com/bumang/movie-app/blob/a27ebfd53beecc3a23da2414e7fe00acc5d814ec/tailwind.config.ts

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FADAI Blue
        primary: {
          DEFAULT: '#2B59FF',
          50: '#F0F4FF',
          100: '#E0E9FF',
          200: '#C1D3FF',
          300: '#A2BDFF',
          400: '#6B8EFF',
          500: '#2B59FF', // Main primary
          600: '#1F3FD6',
          700: '#1428A0',
          800: '#0E176D',
          900: '#080D3A',
        },
        // Background Off-white
        background: {
          DEFAULT: '#FBFBFB',
          light: '#F5F5F7',
        },
        // Text colors
        text: {
          primary: '#1D1D1F',
          secondary: '#86868B',
        },
      },
      fontFamily: {
        sans: ['Inter', ...defaultTheme.fontFamily.sans],
      },
      fontSize: {
        // Use tracking-tight pattern
        xs: ['12px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        sm: ['14px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        base: ['16px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        lg: ['18px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        xl: ['20px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        '2xl': ['24px', { lineHeight: '1.4', letterSpacing: '-0.02em' }],
        '3xl': ['30px', { lineHeight: '1.3', letterSpacing: '-0.02em' }],
        '4xl': ['36px', { lineHeight: '1.2', letterSpacing: '-0.02em' }],
      },
      boxShadow: {
        // Card shadows (Xiaomi style)
        'sm': '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
        'md': '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
        'lg': '0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05)',
        // Soft card shadow
        'card': '0 2px 8px rgba(0, 0, 0, 0.08)',
        // Elevated soft shadow
        'card-lg': '0 8px 16px rgba(0, 0, 0, 0.12)',
      },
      borderRadius: {
        'card': '16px', // Xiaomi card style
        '2xl': '16px',
      },
      animation: {
        'fade-in': 'fadeIn 0.6s ease-out',
      },
      keyframes: {
        fadeIn: {
          '0%': { opacity: '0', transform: 'translateY(20px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
      },
    },
  },
  plugins: [],
};
```


## License: MIT
https://github.com/bumang/movie-app/blob/a27ebfd53beecc3a23da2414e7fe00acc5d814ec/tailwind.config.ts

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FADAI Blue
        primary: {
          DEFAULT: '#2B59FF',
          50: '#F0F4FF',
          100: '#E0E9FF',
          200: '#C1D3FF',
          300: '#A2BDFF',
          400: '#6B8EFF',
          500: '#2B59FF', // Main primary
          600: '#1F3FD6',
          700: '#1428A0',
          800: '#0E176D',
          900: '#080D3A',
        },
        // Background Off-white
        background: {
          DEFAULT: '#FBFBFB',
          light: '#F5F5F7',
        },
        // Text colors
        text: {
          primary: '#1D1D1F',
          secondary: '#86868B',
        },
      },
      fontFamily: {
        sans: ['Inter', ...defaultTheme.fontFamily.sans],
      },
      fontSize: {
        // Use tracking-tight pattern
        xs: ['12px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        sm: ['14px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        base: ['16px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        lg: ['18px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        xl: ['20px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        '2xl': ['24px', { lineHeight: '1.4', letterSpacing: '-0.02em' }],
        '3xl': ['30px', { lineHeight: '1.3', letterSpacing: '-0.02em' }],
        '4xl': ['36px', { lineHeight: '1.2', letterSpacing: '-0.02em' }],
      },
      boxShadow: {
        // Card shadows (Xiaomi style)
        'sm': '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
        'md': '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
        'lg': '0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05)',
        // Soft card shadow
        'card': '0 2px 8px rgba(0, 0, 0, 0.08)',
        // Elevated soft shadow
        'card-lg': '0 8px 16px rgba(0, 0, 0, 0.12)',
      },
      borderRadius: {
        'card': '16px', // Xiaomi card style
        '2xl': '16px',
      },
      animation: {
        'fade-in': 'fadeIn 0.6s ease-out',
      },
      keyframes: {
        fadeIn: {
          '0%': { opacity: '0', transform: 'translateY(20px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
      },
    },
  },
  plugins: [],
};
```


## License: MIT
https://github.com/bumang/movie-app/blob/a27ebfd53beecc3a23da2414e7fe00acc5d814ec/tailwind.config.ts

```
The `tailwind.config.ts` file doesn't exist. I need to create it with the specifications from the frontend agent. Based on the frontend persona, here's the configuration I'll create:

```typescript
import type { Config } from 'tailwindcss';
import defaultTheme from 'tailwindcss/defaultTheme';

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary FADAI Blue
        primary: {
          DEFAULT: '#2B59FF',
          50: '#F0F4FF',
          100: '#E0E9FF',
          200: '#C1D3FF',
          300: '#A2BDFF',
          400: '#6B8EFF',
          500: '#2B59FF', // Main primary
          600: '#1F3FD6',
          700: '#1428A0',
          800: '#0E176D',
          900: '#080D3A',
        },
        // Background Off-white
        background: {
          DEFAULT: '#FBFBFB',
          light: '#F5F5F7',
        },
        // Text colors
        text: {
          primary: '#1D1D1F',
          secondary: '#86868B',
        },
      },
      fontFamily: {
        sans: ['Inter', ...defaultTheme.fontFamily.sans],
      },
      fontSize: {
        // Use tracking-tight pattern
        xs: ['12px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        sm: ['14px', { lineHeight: '1.5', letterSpacing: '-0.01em' }],
        base: ['16px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        lg: ['18px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        xl: ['20px', { lineHeight: '1.6', letterSpacing: '-0.01em' }],
        '2xl': ['24px', { lineHeight: '1.4', letterSpacing: '-0.02em' }],
        '3xl': ['30px', { lineHeight: '1.3', letterSpacing: '-0.02em' }],
        '4xl': ['36px', { lineHeight: '1.2', letterSpacing: '-0.02em' }],
      },
      boxShadow: {
        // Card shadows (Xiaomi style)
        'sm': '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
        'md': '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
        'lg': '0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05)',
        // Soft card shadow
        'card': '0 2px 8px rgba(0, 0, 0, 0.08)',
        // Elevated soft shadow
        'card-lg': '0 8px 16px rgba(0, 0, 0, 0.12)',
      },
      borderRadius: {
        'card': '16px', // Xiaomi card style
        '2xl': '16px',
      },
      animation: {
        'fade-in': 'fadeIn 0.6s ease-out',
      },
      keyframes: {
        fadeIn: {
          '0%': { opacity: '0', transform: 'translateY(20px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
      },
    },
  },
  plugins: [],
};
```

